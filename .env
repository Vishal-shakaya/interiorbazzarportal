# Default flavour = local (in-app mock, no backend). Vite loads this in every mode;
# .env.dev / .env.stage / .env.prod override it for those builds.
# Only VITE_-prefixed vars are exposed to the app.
VITE_DATA_MODE=local
VITE_API_LATENCY=200
