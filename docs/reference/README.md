# reference/ — archived legacy docs

These two files describe the **legacy v3 frontend** (the existing `interiorbazzarfrontend` codebase).
They are kept for **context only** — patterns, naming, and conventions worth borrowing.

They are **superseded** for Version-1 by [../react-project-structure.md](../react-project-structure.md),
which carries forward the good conventions (3-file pattern, services, tokens) and adds the local-first
DataSource seam. When the two disagree, the Version-1 doc wins.

- `React Project Structure.md` — v3 frontend context (routing, layouts, content files, API-swap pattern).
- `React Project Structure1.md` — base conventions (3-file pattern, services, tokens, commands).
